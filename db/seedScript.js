
const seedData = require('./seedData.js');

    seedData.insertSampleUpdates()// Thinking I should convert this to a script in order to seed my database so it doesn't keep seeding it every time I make changes.
    seedData.insertSampleComments() //Thinking I should convert this to a script in order to seed my database so it doesn't keep seeding it every time I make changes.
